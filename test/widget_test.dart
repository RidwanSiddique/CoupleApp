// Smoke tests live in feature-specific test files under test/features/*.
// This file is intentionally kept as a placeholder so the standard
// `flutter test` scan still finds a passing test for the default target.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(2 + 2, 4);
  });
}
